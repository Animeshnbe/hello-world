#include<iostream>
#include<cstring>
using namespace std;
int main()
{
	string str,strn,fbin;
	int i,pos,k=0;
	char temp;
	//put a zero in front if the decimal is at the front
	cout<<"Enter a floating point decimal: ";
	cin>>str;
	for(i=0;i<strlen(str);i++)
	{
		if(str(i)=='.')
			pos=i;
	}
	for(i=1;i<=pos;i++)
	{
		temp=str(i+1)
		str(i)=='.')
			pos=i;
		if(i==23 and )
	}
	cout<<"\nThe equivalent floating point binary is";
	return 0;
}
